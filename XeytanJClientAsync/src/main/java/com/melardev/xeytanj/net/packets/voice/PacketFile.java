package com.melardev.xeytanj.net.packets.voice;

/*
public class PacketFile extends PacketCamera {


    public byte[] data;

    public PacketFile(byte[] _data) {
        super(PacketType.FILE);
        data = _data;
    }

    public PacketFile(File f) {
        super(PacketType.FILE);
        if (f.exists()) {
            try {
                data = Files.readAllBytes(f.toPath());
            } catch (IOException e) {
                e.printStackTrace();
            }
        }
    }
}
*/